node_modules/
dist/
.env
.env.local
*.log
.DS_Store
Thumbs.db
# scratch commit-message files at the repo root only (git commit -F _commitmsg.txt).
# Root-anchored: a bare `_*` also swallowed src/engines/__tests__/ new files.
/_*

# ── python envs / caches ─────────────────────────────
.venv/
__pycache__/
*.pyc

# ── build artifacts (112MB node snapshot) ────────────
build_assets/

# ── SECRETS — never commit (Dhan PIN + TOTP secret + access token) ──
strategy-lab/dhan_config.json
mt5-bridge/dhan_config.json

# ── runtime state & collected market data ────────────
mt5-bridge/risk_state.json
mt5-bridge/nse_holidays.json
strategy-lab/data/
strategy-lab/results/
strategy-lab/paper/
strategy-lab/dhan_scrip_master.csv

# secrets backups + oversized binaries
strategy-lab/dhan_config.json*
!strategy-lab/dhan_config.example.json
build_assets.zip
installer_output/
